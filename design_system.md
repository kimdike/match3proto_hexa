# 헥사 3매치 퍼즐 게임 - 기획서 (메타게임) v0.3

---

## 📋 변경 이력 (Changelog)

### v0.3 - 2026.04.21
- 게임 컨셉 전면 재정의 (점수/3별 → 포켓몬 탐험/수집)
- 스테이지 구조 재설계 (메인/반복 파밍 분리)
- 포켓몬 조우/포획 시스템 정의
- 콤보 시스템 역할 재정의 (점수 → 조우/포획 보정)
- 덱 시스템 정의 (6슬롯, 타입 보너스)
- 포켓몬 성장/진화 시스템 정의
- 하트/블루하트 재화 시스템 정의
- 도감 시스템 상세화
- 경제 구조 정의

### v0.2 - 2026.04.08
- 스프라이트 파일명 변경 반영 (pokemon_sprites.png → pokemon_sprites_1.png)
- 배경 처리 방식 변경 반영 (흰색 → 투명 배경 PNG)
- 블록 스킨 시스템 구현 완료 반영
- 스킨 변경 화면 구현 완료 반영

### v0.1 - 2026.04.04
- 메인화면 / 로비 UI 정의
- 스테이지 구조 정의 (10스테이지, 로비 가운데 현재 스테이지 버튼 1개 방식)
- 블록 스킨 시스템 정의 (포켓몬 스프라이트 시트 151종)
- 도감 시스템 정의 (포켓몬 수집/경험치 개념)
- 상점 시스템 정의 (껍데기)

---

## 1. 게임 컨셉

본 게임은 3매치 퍼즐을 기반으로 한 **포켓몬 탐험 및 수집형 게임**이다.
기존 퍼즐게임의 점수/3별 시스템을 제거하고,
**포켓몬 조우 → 포획 → 도감 수집**을 핵심 루프로 설계한다.

### 핵심 경험
- 기존: 점수 경쟁, 3별 반복 클리어
- 본 게임: 탐험, 조우, 포획, 수집, 전략 덱 구성

### 핵심 플레이 루프
```
퍼즐 플레이 → 클리어 → 포켓몬 조우(확률) → 포획/실패
→ 도감/강화 → 반복 플레이
```

---

## 2. 전체 화면 흐름

```
메인화면
    ↓
캐릭터 선택 (최초 1회)
    ↓
닉네임 입력 (최초 1회)
    ↓
로비
    ├── ADVENTURE START → 퍼즐 플레이 → 클리어/실패 → 조우(확률) → 로비
    ├── 스킨 버튼 → 스킨/덱 변경 화면
    ├── 상점 버튼 → 상점 화면
    ├── 도감 버튼 → 도감 화면
    └── 준비중 버튼
```

---

## 3. 스테이지 구조

### 3-1. 메인 스테이지
- 전진형 1회 클리어 구조 (클리어 후 재입장 불가)
- 순서대로 해금 (이전 스테이지 클리어 후 다음 개방)
- 업데이트를 통해 수천 개 이상으로 확장 예정
- 클리어 보상: 골드, 다이아, 업적 보상
- 로비에서 현재 도전 가능한 스테이지 버튼 1개 표시
- 모든 스테이지 클리어 시 → "모든 스테이지 완료!" 표시

### 3-2. 반복 파밍 스테이지
- 특정 구간 클리어 시 해금 (예: 메인 1~100 클리어 → 풀 지역 해금)
- 기존 메인 스테이지 풀 재사용 (예: 10~99 랜덤)
- 턴 수 감소 (난이도 상승)
- 배경/연출 변경으로 차별화
- 입장 재화: 블루하트 사용
- 보상: 몬스터볼 제작 재료, 타입 성장 재화, 포켓몬 조우 확률 증가

---

## 4. 재화 시스템

### 4-1. 하트 (메인 스테이지 입장권)
- 메인 스테이지 입장 시 소비
- 최대 보유: 5개
- 충전: 10분당 1개 자동 충전
- 5개 초과 보유 시 충전 타이머 정지
- 수급: 자동 충전, 출석, 이벤트

### 4-2. 블루하트 (반복 스테이지 입장권)
- 반복 파밍 스테이지 입장 시 소비
- 최대 보유: 5개
- 충전: 10분당 1개 자동 충전
- 5개 초과 보유 시 충전 타이머 정지
- 수급: 출석, 업적, 이벤트, 반복 보상

### 4-3. 골드
- 최초 0골드에서 시작
- 클리어 보상: 기본 300골드 + 남은 턴 × 5골드
- localStorage에 저장 (키: hexPuzzleGold)
- 로비 상단에 현재 보유 골드 표시
- 사용처: 포켓몬 스킨 구매, 몬스터볼 구매 등 상점에서 사용
- 개발자 모드에서 골드 추가 가능 (테스트용)

### 4-4. 다이아
- 프리미엄 재화
- 메인 스테이지 클리어 보상, 이벤트 지급
- 하트 충전, 프리미엄 몬스터볼 구매 등

### 4-5. 몬스터볼
- 포켓몬 포획 시 사용
- 종류:
  - 기본볼 (BallRate: 0.5)
  - 슈퍼볼 (BallRate: 0.7)
  - 하이볼 (BallRate: 0.9)
  - 타입볼 (특정 타입 포획 확률 상승)
- 수급: 일일 지급, 주간 보상, 이벤트, 반복 스테이지 재료 제작
- 클리어 후 조우 발생 시 기본볼 1개 무료 제공

---

## 5. 포켓몬 조우 시스템

### 5-1. 조우 시점
- 퍼즐 클리어 직후 발생 (입장 시 조우 없음, 어뷰징 방지)
- 조우 미발생 시 → 바로 클리어 화면으로 이동

### 5-2. 조우 확률
| 구분 | 기본 확률 |
|---|---|
| 메인 스테이지 | 4% |
| 반복 파밍 스테이지 | 10% |

### 5-3. 조우 확률 공식
```
FinalEncounterRate =
    BaseRate
    + PityBonus (FailCount × 0.005, 최대 +5%)
    + ComboBonus
    + EventBonus
```

#### 콤보 보너스
| 콤보 | 조우 확률 보너스 |
|---|---|
| 5+ | +0.5% |
| 10+ | +1.0% |
| 15+ | +1.5% |

### 5-4. 희귀도 테이블
| 등급 | 기본 확률 |
|---|---|
| 일반 | 80% |
| 레어 | 18% |
| 희귀 | 2% |

- 콤보에 따라 레어/희귀 확률 상승

### 5-5. 조우 화면 흐름
```
퍼즐 클리어
    ↓
조우 발생(확률) → 조우 화면 등장
"야생 ○○이 나타났다!"
    ↓
[몬스터볼 선택]   [스킵]
    ↓              ↓
포획 시도        도감 "발견" 등록
    ↓
성공/실패
    ↓
클리어 화면

조우 없음 → 바로 클리어 화면
```

- 스킵 시: 도감에 "발견" 상태로 등록 (포획 미완료)

---

## 6. 포획 시스템

### 6-1. 포획 확률 공식
```
CatchRate =
    BallRate
    × MonsterDifficulty
    × ComboBonus
    × StackBonus (1 + FailStack × 0.05)
```

#### MonsterDifficulty
| 등급 | 난이도 |
|---|---|
| 일반 | 1.0 |
| 레어 | 0.8 |
| 희귀 | 0.6 |

### 6-2. 포획 실패 보정
- 실패 시 다음 동일 포켓몬 조우 시 포획 확률 +5% (스택형)
- 도감에 "발견" 상태로 등록

---

## 7. 콤보 시스템

### 7-1. 역할 재정의
- 점수 시스템 제거로 콤보의 역할 변경
- 콤보 = 조우/포획 보정 요소

### 7-2. 콤보 효과
- 조우 확률 증가 (섹션 5-3 참고)
- 레어/희귀 등급 조우 확률 상승
- 포획 확률 증가

---

## 8. 덱 시스템

### 8-1. 기본 구조
- 6개 블록 슬롯 (기존 7개 → 6개로 변경)
- 각 슬롯에 포켓몬 스킨 장착
- 장착된 포켓몬의 타입이 덱 타입을 결정

### 8-2. 타입 보너스
- 동일 타입 수 증가 시 보너스 증가

| 동일 타입 수 | 보너스 |
|---|---|
| 2개 | +5% |
| 4개 | +10% |
| 6개 | +20% |

### 8-3. 혼합 보너스
- 특정 타입 조합 시 추가 보너스 발동
- 예: 풀 3 + 물 3 → 추가 보너스
- 특정 조합 → 특수 효과 발동

### 8-4. 상성 패널티
- 상성이 맞지 않는 타입 혼합 시 효율 감소
- 예: 불 + 풀 혼합 시 효율 감소

---

## 9. 포켓몬 스킨 시스템

### 9-1. 스프라이트 시트
- 파일: `pokemon_sprites_1.png` (3228×2375px)
- 1마리 크기: 215×215px (가로 15마리 × 세로 11줄 = 151종)
- 배경: 투명 PNG, clip-path로 헥사 셀에 맞게 처리

### 9-2. 기본 해금 스킨
- 7종 → 6종으로 변경
- 기본 해금 6종: 1번, 4번, 7번, 10번, 15번, 25번
- 나머지 145종은 잠금 상태 (🔒 표시)
- 6개 슬롯에 기본 해금 스킨 자동 장착 상태로 시작

### 9-3. 스킨 변경 화면
- 6개 슬롯 표시
- 슬롯 클릭 → 151종 목록 표시
  - 해금 스킨: 컬러로 표시, 클릭 시 장착
  - 잠금 스킨: 어둡게 표시 + 🔒, 클릭 불가
- 변경 즉시 퍼즐에 반영

---

## 10. 포켓몬 성장 시스템

### 10-1. 중복 포획 처리
- 동일 포켓몬 재획득 시 shard 지급

### 10-2. shard 활용
- shard → 포켓몬 강화
- 일정 수량 → 진화 가능

### 10-3. 진화 구조
- 진화체/비진화체 혼재 (151마리 기준)
- 야생에서 진화체 직접 조우 가능
- shard로 진화 시 도감에 별도 등록

### 10-4. 이로치
- 희귀 색상 변형 (이로치) 존재
- 별도 도감 등록

---

## 11. 도감 시스템

### 11-1. 포켓몬 상태
| 상태 | 설명 |
|---|---|
| 미발견 | 아직 조우한 적 없음 (실루엣) |
| 발견 | 조우했으나 포획 실패/스킵 |
| 포획 | 포획 완료 |
| 강화 | shard로 강화된 상태 |

### 11-2. 도감 UI
- 151종 포켓몬 표시
- 미발견: 실루엣/회색
- 발견/포획: 컬러로 표시
- 클릭 시 상세 정보 (타입, 포획 확률, failStack 등)

### 11-3. 추적 표시
- 각 포켓몬별 다음 포획 확률 표시
- 예: "이상해씨 (미포획) - 다음 포획 확률 +10%"

### 11-4. 클리어 화면 연동
- 클리어 화면에 "이 지역 미발견 포켓몬 N마리" 상시 표시
- 수집 동기 부여

### 11-5. 데이터 구조
```
MonsterDex {
    id,
    discovered,
    owned,
    failStack,
    shardCount,
    level
}
```

---

## 12. 로비 시스템

### 12-1. 로비 UI 구성
- 상단: 캐릭터 프로필 + 닉네임 + KANTO LEAGUE 레이블
- 중앙: 선택한 캐릭터 일러스트
- 하단: ADVENTURE START 버튼 + 현재 스테이지 번호
- 하단 탭: 스킨 / 상점 / 도감 / 준비중

### 12-2. 포켓몬 로비 배치 (추후)
- 획득한 포켓몬을 로비에 배치 가능
- 일정 수 제한
- 자유 이동 연출

---

## 13. 상점 시스템

### 13-1. 구성
- 포켓몬 스킨 골드로 구매
- 몬스터볼 구매 (기본볼/슈퍼볼/하이볼/타입볼)
- 다이아로 프리미엄 아이템 구매

---

## 14. 개발 진행 현황
1. ✅ 메인화면 (배경/로고/BGM)
2. ✅ 캐릭터 선택 + 닉네임 입력
3. ✅ 로비 UI (크림/옐로우 톤, 캐릭터 일러스트)
4. ✅ 스테이지 구조 (메인 스테이지 버튼 1개 방식)
5. ✅ 블록 스킨 시스템 (pokemon_sprites_1.png)
6. ✅ 스킨 변경 화면 (7슬롯 → 6슬롯 변경 예정)
7. ⬜ 하트/블루하트 시스템
8. ⬜ 포켓몬 조우 시스템
9. ⬜ 포획 시스템
10. ⬜ 도감 시스템 (상세)
11. ⬜ 덱 시스템 (타입 보너스)
12. ⬜ 포켓몬 성장/진화
13. ⬜ 상점 시스템 (골드/다이아 연동)
14. ⬜ 반복 파밍 스테이지
15. ⬜ 로비 포켓몬 배치
