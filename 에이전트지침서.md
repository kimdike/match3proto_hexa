# 헥사 3매치 퍼즐 - 클로드 코드 작업 문서

## 📌 세션 시작 시 컨텍스트 (매번 붙여넣기)

```
기획서.md 읽고 현재 game.js, index.html, style.css 파일 분석 후
현재 구현 상태 파악하고 작업 이어서 진행해줘.
```

---

## 🤖 멀티에이전트 구조

### 에이전트 1: 게임 로직 담당
```
너는 게임 로직 전담 에이전트야. game.js 파일만 담당해.
UI나 스타일은 건드리지 마.

담당 작업:
- 헥사 그리드 좌표 계산
- 6방향 매치 감지 알고리즘
- 블록 swap 로직
- 특수블록 생성/발동/교차 로직
- 콤보 시스템
- 점수 계산
- 블록 낙하 보충 로직
- 실시간 매칭 입력 버퍼링
```

### 에이전트 2: UI/렌더링 담당
```
너는 UI/렌더링 전담 에이전트야.
index.html, style.css 파일만 담당해. 게임 로직은 건드리지 마.

담당 작업:
- 헥사 셀 시각적 렌더링
- 블록 색상/모양 표시
- 특수블록 비주얼 (줄볼 선, 타겟볼 점, 폭탄볼 방사형, 무지개볼 그라디언트)
- 점수판/HUD UI
- 속도 조절 슬라이더 UI
- 게임 시작/종료 화면
- 애니메이션 효과
```

### 에이전트 3: 디버그/테스트 담당
```
너는 디버그/테스트 전담 에이전트야.
코드를 수정하지 말고 문제점만 찾아서 보고해.

담당 작업:
- 에러 메시지 분석
- 버그 원인 파악
- 수정 방향 제안
- 특정 기능 동작 여부 확인
```

---

## 📋 남은 작업 프롬프트

### 매칭 계산 속도 개선
```
기획서.md와 game.js 읽고 매칭 계산 속도를 개선해줘.

현재 문제: 매치 감지 → 제거 → 낙하를 순차적으로 처리해서 느림
개선 방향: 배치 처리 방식으로 변경
- 매치 감지 → 동시에 전체 처리 → 한번에 낙하 → 반복
- 연쇄 콤보도 빠르게 처리되도록 최적화
```

### 속도 조절 슬라이더 UI
```
기획서.md와 game.js, index.html, style.css 읽고
우측 UI에 속도 조절 슬라이더 2개 추가해줘.

1. 블록 낙하 속도 슬라이더
   - 범위: 0.5x ~ 3x
   - 숫자 직접 입력도 가능
   - 실시간 변경 즉시 반영

2. 타겟볼 날아가는 속도 슬라이더
   - 범위: 0.5x ~ 3x
   - 숫자 직접 입력도 가능
   - 실시간 변경 즉시 반영
```

### 폭탄볼 생성 위치 수정
```
기획서.md와 game.js 읽고 폭탄볼 생성 위치 로직 수정해줘.

현재 문제: 생성 위치가 부정확함
수정 목표: 매칭된 블록들의 교차로(중심) 부근에 생성
- ㄴ자 매칭 → 코너 위치
- T자 매칭 → 가운데 위치
- X자 매칭 → 중심 위치
교차로 부근에 1개만 생성
```

### 실시간 매칭 (입력 버퍼링)
```
기획서.md와 game.js 읽고 실시간 매칭 시스템 구현해줘.

목표: 블록 낙하/연출 중에도 swap 조작 가능

조작 가능한 상황:
- 콤보 연쇄 중
- 블록 낙하 애니메이션 중
- 일반 특수블록 연출 중

조작 불가능한 상황:
- 무지개볼 사용 시
- 무지개볼 + 특수블록 교차 시

구현 방식: 입력 큐(Input Queue) 방식
- 조작 가능 상황에서 swap 시도 → 즉시 실행
- 조작 불가 상황에서 swap 시도 → 무시
```

### 특수블록 구현 현황 체크
```
기획서.md와 game.js 읽고 특수블록 구현 현황 체크해줘.

확인 항목:
- 줄볼: 생성/발동/방향/비주얼 정상 동작 여부
- 타겟볼: 생성/발동/랜덤타겟/비주얼 정상 동작 여부
- 폭탄볼: 생성/발동/폭발범위/비주얼 정상 동작 여부
- 무지개볼: 생성/발동1(swap)/발동2(타격)/연출/비주얼 정상 동작 여부
- 교차 효과 9가지 구현 여부
- 예외1 (특수블록이 다른 특수블록 타격 시 발동) 구현 여부
- 예외2 (특수블록 두 개 직접 swap 가능) 구현 여부
```

---

## 🐛 에러 발생 시 프롬프트
```
game.js 읽고 아래 에러 원인 파악하고 수정해줘.

에러 메시지: [에러 내용 붙여넣기]
발생 상황: [어떤 동작 했을 때]
```

---

## 📅 추후 작업 (데모 완성 후)
- 리팩토링 & 모듈화
  - game.js → grid.js / block.js / match.js / special.js / animation.js / ui.js 로 분리

---

## 💡 팁
- 매 세션 시작 시 컨텍스트 먼저 붙여넣기
- 한 단계씩 완료 확인 후 다음 단계로 이동
- 에러나면 F12 → Console 탭에서 에러 복사 후 클로드 코드에 붙여넣기
- 브라우저에서 F5로 변경사항 확인
